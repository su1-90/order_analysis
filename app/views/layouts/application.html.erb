<!DOCTYPE html>
<html>
<head>
  <title>Order Analysis</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body class="d-flex flex-column min-vh-100">
  <% if !current_page?(root_path) && !current_page?(new_user_session_path) %>
    <header>
      <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="#">Order Analysis</a>
        <ul class="navbar-nav ml-auto">
        </ul>
      </nav>
    </header>
  <% end %>

  <% flash.each do |message_type, message| %>
    <div class="alert alert-<%= message_type %> alert-dismissible fade show" role="alert">
      <%= message %>
      <button type="button" class="close" data-dismiss="alert" aria-label="Close">
        <span aria-hidden="true">&times;</span>
      </button>
    </div>
  <% end %>

  <div class="container-fluid flex-grow-1">
    <div class="row">
      <%= yield %>
    </div>
  </div>

  <footer class="footer mt-auto py-3 bg-light">
    <div class="container text-right">
      <span class="text-muted text-right">© Order Analysis 2025</span>
    </div>
  </footer>

  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script> <%# jQueryの読み込み %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %> <%# JavaScriptの読み込み %>
</body>
</html>